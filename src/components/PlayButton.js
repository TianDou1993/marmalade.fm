import React from 'react';

const PlayButton=props =>(
    <div>
        <button className="button bg-transparent play-button button-reset contain db bg-center pointer center"/>
  </div>)

export default PlayButton